<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SEKeyboardForPassport" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="PopupButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:background">#555555</item>
        <item name="android:layout_margin">2dp</item>
    </style>

    <style name="Theme.SEKeyboardForPassport" parent="Base.Theme.SEKeyboardForPassport" />
</resources>